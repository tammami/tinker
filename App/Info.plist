<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Merged into the generated Info.plist. SQLite database files open as connections;
	     the rank is Alternate so Tinker offers itself without claiming the files. -->
	<!-- Sparkle (SPEC §2.1). The feed and key come from the TINKER_APPCAST_URL and
	     TINKER_SPARKLE_PUBLIC_KEY build settings; Updater reports "not configured" when
	     either is empty. Automatic checks are off until the user turns them on in Settings,
	     so Sparkle never asks with its own prompt and never reaches the network unasked. -->
	<key>SUFeedURL</key>
	<string>$(TINKER_APPCAST_URL)</string>
	<key>SUPublicEDKey</key>
	<string>$(TINKER_SPARKLE_PUBLIC_KEY)</string>
	<key>SUEnableAutomaticChecks</key>
	<false/>
	<key>SUEnableInstallerLauncherService</key>
	<false/>
	<key>SUVerifyUpdateBeforeExtraction</key>
	<true/>
	<key>CFBundleDocumentTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeName</key>
			<string>SQLite Database</string>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>LSHandlerRank</key>
			<string>Alternate</string>
			<key>CFBundleTypeExtensions</key>
			<array>
				<string>sqlite</string>
				<string>sqlite3</string>
				<string>db</string>
				<string>db3</string>
				<string>s3db</string>
				<string>sl3</string>
			</array>
		</dict>
		<dict>
			<key>CFBundleTypeName</key>
			<string>SQL Script</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>LSHandlerRank</key>
			<string>Alternate</string>
			<key>CFBundleTypeExtensions</key>
			<array>
				<string>sql</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
