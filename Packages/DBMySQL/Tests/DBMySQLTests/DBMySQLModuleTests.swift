import XCTest
@testable import DBMySQL

/// Phase 0 smoke test: the module links and its test target runs.
final class DBMySQLModuleTests: XCTestCase {
    func testModuleLinks() {
        XCTAssertTrue(true)
    }
}
