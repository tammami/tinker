import XCTest
@testable import DBStore

/// Phase 0 smoke test: the module links and its test target runs.
final class DBStoreModuleTests: XCTestCase {
    func testModuleLinks() {
        XCTAssertTrue(true)
    }
}
