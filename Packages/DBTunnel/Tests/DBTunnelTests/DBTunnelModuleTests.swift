import XCTest
@testable import DBTunnel

/// Phase 0 smoke test: the module links and its test target runs.
final class DBTunnelModuleTests: XCTestCase {
    func testModuleLinks() {
        XCTAssertTrue(true)
    }
}
